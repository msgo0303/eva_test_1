# [기능 추가] "금일 활동 불가" 계획 등록 및 미계획 인원 제외 기능

현재 계획이 없는 인원을 챙기는 시스템에서, 부득이한 사유로 활동이 불가능한 인원이 '미계획 인원(미취합자)'으로 오집계되어 피로도가 발생하는 문제를 해결하기 위해, 활동 종류에 **"금일 활동 불가"**를 추가하고 사유를 작성하여 미계획 인원에서 정상적으로 제외될 수 있도록 개선합니다.

## User Review Required

> [!IMPORTANT]
> - **데이터 저장 방식**: "금일 활동 불가" 카드는 활동 계획 제출을 완료한 상태로 판단하여 DB 상에 `status: 'completed'` 및 `content: '금일 활동 불가'`로 저장합니다. 
> - **기본값 할당**: 시간 필드의 DB 제약조건 충족 및 시스템 오작동 방지를 위해, 시간은 `"00:00"` ~ `"00:00"`으로 자동 저장됩니다.
> - **통계 분류**: 대시보드 통계 계산 시 "금일 활동 불가" 카드는 '완료' 건수에서 제외하고, 사용자 요청 사항("취소와 비슷하게 처리")에 맞추어 **'취소' 통계 수치에 합산**하거나 별도 처리합니다.

## Open Questions

> [!NOTE]
> 1. **통계 계산 분류**: "금일 활동 불가" 카드를 대시보드 메인 통계의 **'취소'** 수치에 합산하여 표시할지, 아니면 아예 통계 산출(완료/계획/취소)에서 스킵할지 의견을 듣고 싶습니다. (현재 계획은 '취소'에 합산하여 보여주는 방향입니다.)

## Proposed Changes

### Frontend Components

---

#### [MODIFY] [index.html](file:///c:/Users/gmdoh/Desktop/eva_test/index.html)

- **활동 내용 셀렉트 박스(`f-content-category`) 수정**:
  - `<option value="금일 활동 불가">금일 활동 불가</option>` 항목 추가.
- **활동 모달 입력 폼 동적 처리**:
  - `f-content-category`가 `"금일 활동 불가"`로 변경될 경우:
    - 활동 시간 선택 영역(`f-starttime`, `f-endtime`) 숨김 처리.
    - 장소 입력 필드(`f-location`)의 라벨을 `"활동 불가 사유"`로 변경하고 placeholder를 `"예: 개인 일정, 건강 문제 등 사유 입력"`으로 변경.
    - 진행 상태 세그먼트(`status-seg`), 활동 결과 영역(`result-box`), 활동 피드백(`f-result-text`) 등 불필요한 입력 란 숨김 처리.
    - "계획 저장" / "결과 저장" 버튼 라벨을 `"활동 불가 등록"`으로 일원화.
  - 저장 유효성 검사(`saveActivity`) 시:
    - `"금일 활동 불가"`일 때는 시간 유효성 검사를 스킵하고 `startTime`, `endTime`을 `"00:00"`으로 자동 세팅.
    - 장소(사유) 필드가 비어있지 않은지 검사 (사유 필수 입력).
    - `formStatus`를 무조건 `'completed'`로 강제하여 미취합자 체크 시 완료된 건으로 인식되도록 처리.
- **활동 카드 렌더링 (`renderActivities` 등) 수정**:
  - 카드의 `content`가 `"금일 활동 불가"`인 경우:
    - 카드의 불투명도를 낮춰(`opacity-60`) 취소 카드와 비슷하게 처리.
    - 우측 상단 뱃지에 `"활동 불가"`로 표기하며, 스타일은 `badge-canceled` (또는 유사한 연한 빨간색/회색 스타일) 적용.
    - 시간 및 장소 핀 아이콘 대신 **안내 아이콘**과 함께 **"사유: [입력한 사유]"** 문구만 노출.
    - 카드 수정 모달 진입 시 상태 복원 기능 대응.
- **대시보드 통계 반영**:
  - `stat-planned`, `stat-completed`, `stat-canceled` 카운팅 로직에서 `content === '금일 활동 불가'` 분기 처리.

## Verification Plan

### Automated Tests
- 없음 (로컬 프론트엔드 및 API 동작 확인 필요)

### Manual Verification
1. **활동 등록 모달 테스트**:
   - 새 활동 추가 시 "금일 활동 불가" 선택 시 폼 필드가 변경되는지 확인 (시간 숨김, 사유 입력창 활성화).
   - 사유 미입력 상태로 저장 시 경고창이 뜨는지 확인.
   - 사유 입력 후 저장 시 DB에 정상 반영되는지 확인.
2. **미취합자 제외 검증**:
   - "금일 활동 불가" 등록 직후 대시보드 탭의 "오늘 결과 미취합자" 및 "내일 계획 미취합자" 명단에서 본인이 제외되는지 확인.
3. **카드 렌더링 검증**:
   - 홈 화면에서 "금일 활동 불가" 카드가 `opacity-60`과 "활동 불가" 뱃지 및 사유 형태로 취소 카드처럼 시각화되는지 확인.
4. **대시보드 통계 검증**:
   - 통계 화면의 완료/계획/취소 수치가 설계대로(취소에 합산 혹은 완료 제외) 반영되는지 확인.
