# [프로젝트 개요 및 Antigravity 연속 작업 지시서]

**프로젝트**: 텔레그램 연동 팀 활동 관리 웹앱 (Google Apps Script / WebApp 기반)  
**목적**: 조원들의 일별 활동 계획/결과 입력, 관리자 대시보드(통계, 미취합/미달성 추적, 멤버 관리, 개강 사이클 설정) 관리 시스템

---

## 1. 현재까지 완료된 작업 (Completed Status)

### [Phase 1] 메인 UI/UX 개선 & 0건 결과 입력
- 계획중/완료/취소 필터링 및 카테고리별/세부유형별 결과 선택 UI 구현.
- 찾아낸 수량이 0건이어도 기록 가능하도록 0~10개 선택 옵션 제공.

### [Phase 2] 복방 수치화, 듀얼 미취합 & Calendar 시트 연동
- **Calendar 시트 연동**: B열 'OFF' 설정 시 공식 비계수일/휴무일로 판단하여 대시보드 미취합 집계 자동 스킵.
- **듀얼 미취합 추적**: [선택 날짜 당일 결과 미취합자] + [내일 계획 미취합자] 분리 집계.
- **복방 보유 수치화 및 주간 미션**: 유저별 복방 보유 수량 관리(N열) 및 복방 1개 이상 보유 시 주간 미션(찾기 6건) 자동 면제 UI 반영.

### [Phase 3] 개강 사이클 관리 & 관리자 홈/대시보드 지역 필터 적용
- 개강 사이클(Semesters 시트) 설정 UI 및 백엔드 API 구현.
- **14개 고정 지역 배열 적용**: `["사당", "안양", "신림", "신사", "금천", "군포", "인덕원", "잠실", "양재", "약수", "서울시흥", "서울역", "새신자", "대학"]`
- 관리자 권한 시 홈 화면('오늘의 활동 계획') 및 대시보드 각 탭(미취합, 멤버 관리)에서 선택한 지역별 유저/카드만 필터링되도록 처리.
- 카드 및 모든 리스트 명단에서 `[조|지역] 병행 표기` (예: `[1조|사당] 홍길동`).
- 홈 화면 날짜 텍스트 클릭 시 Native Date Picker(달력 팝업)를 통해 임의 날짜로 즉시 이동하는 기능 구현.

### [Phase 3.5] 공지사항 시스템 및 대시보드 사용성(접기/펴기) 개선
- **공지사항(Notices) 게시판 연동**: 홈 화면 아코디언형 공지 세로 리스트 구현, 읽음 여부(New 배지) 캐싱, 공지 상세 보기 팝업 및 공지 작성(조별 권한 보안 처리) 기능 추가.
- **날짜 표기 및 텍스트 정밀화**: 홈 화면 날짜에서 주차 표시를 생략하여 가독성 개선, 대시보드 KPI 카드 및 세부 칩의 명칭과 이모지 수정.
- **미취합 접기/펴기 기능**: 대시보드 내 미취합자 목록 토글 지원 및 상태 보존 처리.

### [Phase 3.6] 조(Group) → 지역(Region) 권한 및 대시보드 마이그레이션 완료
- **권한 범위 전환**: 조장/부조장 권한의 사용자가 오늘 활동 및 공지사항을 볼 때, 기존 조 기준(`rowGroup === userObj.group`)이 아닌 **소속 지역 기준(`rowRegion === userObj.region`)**으로 대상이 제한되도록 변경했습니다.
- **백엔드 보안 강화**: `getDashboardData`, `getRiskData` API 호출 시 조장 계정인 경우 **서버 단에서 타 지역 유저의 활동 및 미취합 데이터를 원천 차단**하여 본인 지역 데이터만 안전하게 반환하도록 보강했습니다.
- **대시보드 통계 및 그래프 전환**: 대시보드의 '조별 기여도' 차트와 KPI 1위 통계를 **'지역별 기여도' 차트 및 '1위 지역' 통계**로 전면 교체하여 14개 고정 지역 기준의 실시간 기여 순위를 출력합니다.
- **지역 필터 잠금 기능**: 조장/부조장 로그인 시 홈 화면 및 대시보드 탭의 지역 선택 셀렉트 박스(`select`)들이 **본인의 소속 지역으로 자동 세팅 및 비활성화(disabled) 처리**되어 타 지역 데이터 접근을 방지하는 UX를 구현했습니다.

---

## 2. 앞으로 진행할 남은 계획 (Roadmap)

### [Phase 4] Supabase 초고속 DB 이관 (성능 최적화)
- 현재 구글 앱스 스크립트(GAS) 및 구글 시트 통신 지연(1~2.5초)으로 인해 대시보드 멤버 관리 버튼 반응 속도가 느린 현상 해결.
- Supabase (PostgreSQL) 스키마 구성 및 프론트엔드 통신 레이어 이관.
- 낙관적 UI(Optimistic UI) 적용을 통해 버튼 클릭 즉시 화면 UI가 변경되도록 고속화.

### [Phase 5] 텔레그램 Bot API 직발송 연동 및 필드 테스트
- 대시보드 미취합자/미달성자 탭의 [독촉 알림] 클릭 시, 클립보드 복사뿐만 아니라 텔레그램 그룹방으로 Bot API를 통해 자동 메시지 발송 기능 구현.
- 실제 리더진 및 사용자 대상 최종 안정화 및 예외 처리.
